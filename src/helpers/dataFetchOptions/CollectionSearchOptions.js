class CollectionSearchOptions {
  constructor(sortOption, pageSize, pageOffset) {
    this.sortOption = sortOption;
    this.pageSize = pageSize;
    this.pageOffset = pageOffset;
  }
}

module.exports = CollectionSearchOptions;
